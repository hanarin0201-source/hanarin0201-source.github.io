# 콕매치 버전 관리 규칙

1. 기능 변경 전 현재 운영본을 `backup/vNN` 브랜치로 고정 백업한다.
2. 동시에 `versions/vNN/` 폴더에 해당 운영본 파일을 보관한다.
3. `versions/vNN/` 아래 보관된 파일과 `backup/vNN` 브랜치는 이후 기능 개발에서 수정하거나 덮어쓰지 않는다.
4. 새 기능은 다음 버전 번호의 신규 파일로 작성한다. 예: v21 운영 중 다음 기능 변경은 v22.
5. 운영본 변경 시 설정의 프로그램 버전 표시, `/versions/` 버전 보관함, 서비스워커 캐시 버전을 함께 갱신한다.
6. 구버전은 `/versions/vNN/index.html`에서 열어 확인할 수 있게 유지한다.
7. 서버 구조 변경으로 구버전 호환성이 깨질 가능성이 있으면 서버 변경 전에 별도 호환 계층 또는 서버 백업을 먼저 만든다.
